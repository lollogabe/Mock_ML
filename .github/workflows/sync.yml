name: Sync Colab

on:
  push:
    branches: [main, develop]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Colab
        run: echo "Changes pushed - Pull latest in Colab"
